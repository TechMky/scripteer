{
  "name": "scripteer",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://techmky.github.io/scripteer",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "predeploy": "cd client && npm run build",
    "deploy": "cd client && gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
